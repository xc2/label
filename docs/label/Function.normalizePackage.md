[@109cafe/label](index.md) / normalizePackage

# Function: normalizePackage()

> **normalizePackage**(`path`): `string`

remove the leading `./` in the package path for label specs

## Parameters

• **path**: `string`

the package path

## Returns

`string`
